<% if @fact.valid? %>
  <% if @fact.lean? %>
	    $('#js-pro-facts').prepend("<%= escape_javascript(render(@fact)) %> ");
  <% else %>
      $('#js-con-facts').prepend("<%= escape_javascript(render(@fact)) %> ");
  <% end %>
	$('#new-fact').html("<%= escape_javascript(render partial: 'facts/form', locals: { topic: @topic, fact: @fact}) %>");
<% else %>
	$('#new-fact').html("<%= escape_javascript(render partial: 'facts/form', locals: { topic: @topic, fact: @fact}) %>");
<% end %>
